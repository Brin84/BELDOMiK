# BELDOMiK Backend

Telegram Mini App Real Estate Marketplace for Belarus.

FastAPI + SQLAlchemy + Alembic backend. Serves both the REST API (`/api/v1`)
and the built React SPA (copied to `frontend/dist` in the container image).
